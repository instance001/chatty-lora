# Glossary (Repo Excerpt)

For the full glossary, see: https://github.com/instance001/Whatisthisgithub/blob/main/GLOSSARY.md

This file contains only the glossary entries for this repository. Mapping tag legends and global notes live in the full glossary.

## chatty-lora
| Term | Alternate term(s) | Alt map | External map | Relation to existing terminology | What it is | What it is not | Source |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Chatty-lora | chatty lora | ~ | ~ | Local LoRA workflow dashboard | Local-first dashboard for respectful material search, dataset curation, training-plan preparation, and source-specific crawl repair | Not a cloud trainer; not the same app as Chatty-art | chatty-lora/README.md; chatty-lora/USER_MANUAL.md |
| Materials page | materials | ~ | ~ | Dataset intake / curation surface | UI page for searching enabled sources in small batches, previewing results, and curating selected media into named dataset folders under `inputs/` | Not the training runner itself; not a bulk unsupervised crawler | chatty-lora/README.md |
| Selection tray | tray | ~ | ~ | Staging area for chosen search results | Lightweight holding area for previewed media before it is curated into a dataset folder | Not a permanent library; not the final dataset manifest | chatty-lora/README.md |
| Local folder cleanup | clean folder into dataset | ~ | ~ | Manual dataset normalization workflow | Path that takes a user-collected folder under `inputs/` and creates a cleaned dataset copy with normalized filenames, buckets, sidecar captions, and `metadata.json` | Not web crawling; not destructive to the original folder | chatty-lora/README.md |
| Builder page | builder | ~ | ~ | Training-plan composition surface | UI page where the user picks dataset, base model family, backend, concept stack, and starter settings, then saves a reusable training plan | Not live training by itself; not limited to one concept block | chatty-lora/README.md |
| Concept stack | concept blocks | ~ | ~ | Structured training-intent representation | Block-based representation of what the LoRA should learn, split into primary, supporting, and avoid-style components instead of one flat description | Not a single freeform caption; not the final trainer metadata file by itself | chatty-lora/README.md |
| Concept block role | primary lesson, supporting detail, avoid | ~ | ~ | Structured role labeling for concept blocks | Role marker that tells Chatty-lora whether a block is the core lesson, a supporting cue, or a guardrail that should stay out of the positive caption recipe | Not backend choice; not a dataset-quality rating | chatty-lora/README.md |
| Training lane | lane | ~ | ~ | Family-plus-backend route | Explicit pairing of a model family, backend, and dataset kind that keeps Wan, Flux, and future routes conceptually separate under one app | Not just a preset; not a simultaneous multi-backend run | chatty-lora/README.md |
| Wan handoff folder | generated handoff | ~ | ~ | Generated trainer artifact bundle | Folder under `config/training/generated/` containing `dataset.toml`, metadata JSONL, helper scripts, `plan.json`, and a generated `README.md` for the chosen Wan/Musubi run | Not the trained LoRA itself; not an opaque binary export | chatty-lora/README.md |
| Wan preflight card | preflight card | ~ | ~ | Readiness summary surface | Builder/run card that checks model files, WSL, Musubi readiness, and dataset shape before the user starts a run | Not a benchmark; not proof of final LoRA quality | chatty-lora/README.md |
| Run this saved plan | saved-plan runner | ~ | ~ | In-app staged trainer launcher | App runner that executes the saved Wan/Musubi workflow as separate internal stages: preflight, latent cache, text cache, then training | Not arbitrary shell access; not a multi-job scheduler | chatty-lora/README.md |
| Respectful crawling | conservative crawling | ~ | ~ | Low-impact source collection policy | Small-batch, review-first search approach intended to reduce hammering, bans, and accidental overreach when gathering training materials | Not a site-wide scraper vacuum; not a bypass for anti-bot protections or login walls | chatty-lora/README.md |
| Scoped Site-Fix Shell | site-fix shell | ~ | ~ | One-source repair workflow | Review-first flow for inspecting one source, drafting a source-specific fix, previewing a backup-first patch, and applying it only to that source's adapter or connection profile | Not a crawler-core rewrite tool; not a broad auto-fix engine | chatty-lora/README.md |
| Homepage rescue | bounded homepage rescue | ~ | ~ | Generic-source fallback scan | Small same-site follow-up pass used when a generic source homepage has no direct media, so nearby gallery/media/feed/sitemap links can be checked cautiously | Not a deep site crawler; not guaranteed coverage for JS-heavy or protected sites | chatty-lora/README.md |
| Auto-suggested backend | manual override | ~ | ~ | Family-aware backend recommendation state | Visible state showing whether the selected backend came from Chatty-lora's compatibility suggestion or from an intentional manual override | Not a hidden silent switch; not a guarantee the backend is optimal for every dataset | chatty-lora/README.md |
| ECG Window (Chatty-lora) | training activity graph | ~ | ~ | Honest local activity indicator | Small CPU/GPU graph shown on the top runnable saved-plan card so cache stages and live training activity stay visible | Not deep telemetry; not a performance profiler | chatty-lora/README.md |
